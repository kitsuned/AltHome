extends:
  - ../../.eslintrc.yaml

parser: '@typescript-eslint/parser'
parserOptions:
  project: ./tsconfig.json
  ecmaVersion: 2020
  ecmaFeatures:
    jsx: true

env:
  browser: true

settings:
  react:
    version: detect
